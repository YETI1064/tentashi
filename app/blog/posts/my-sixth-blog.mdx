---
title: 'My sixth blog at my personal webspace'
publishedAt: '2024-11-13'
summary: 'Explore the enduring debate between using spaces and tabs for code indentation, and why this choice matters more than you might think.'
---

```
function  (root, value){
if (root === null) return new Node(value);  // Base case: insert the value

    if (value < root.value) {
        root.left = insert(root.left, value);  // Go left
    } else {
        root.right = insert(root.right, value);  // Go right
    }

    return root;
}
```

Well this one is only the first part of binery search tree, so stay tuned for more information!!!
